<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[RSA 非对称加密]]></title>
    <url>%2Fposts%2Frsa-asymmetric-encryption.html</url>
    <content type="text"><![CDATA[对称加密作为最快速、最简单的加密方式, 所以被广泛的使用在很多加密协议的核心中. 但是对称加密也有一个致命的弱点, 那就是对称加密算法在加密和解密是需要使用到同一个 密钥; 当 A 和 B 两个人在需要进行加密数据的传输时, 如果他们使用的是对称加密算法, 那么必须有一端将 密钥 发送给另外一端. 在不安全的网络传输中这一点就是相当危险! 密钥 一旦被窃或拦截, 那么之前所做的加密就相当于已经失效. 1976 年, 美国学者 Dime 和 Henman 为了解决信息公开传送和秘钥管理问题, 提出一种新的密钥交换协议, 允许在不安全媒体上的通信双方交换数据, 安全的达成一致的密钥, 这种密钥交换协议就被称为 “公开密钥密码学 (Public-key cryptography)”. 相对于 对称加密算法 这种方法也被叫做 非对称加密算法. 非对称加密算法之所以可以规避对称加密的致命缺点, 就是因为非对称加密使用的是 一对 密钥, 而不是像对称加密一样使用 一个 密钥; 非对称加密的秘钥分为两个: 一个是公钥 (public key); 另一个则是私钥 (private key). 私钥只由安全的一方保管, 不能外泄; 公钥则可以发给任何请求它的人. 非对称加密算法在加密时可以使用两个密钥中的任何一个, 但是在解密时则需要另外一个密钥才能解密. 比如: 使用公钥加密就需要私钥才能解密、使用私钥加密就需要公钥才能解密. RSA 非对称加密算法非对称加密的主要算法有: RSA、Elgamal、背包算法、Rabin、D-H、ECC (椭圆曲线加密算法), 其中 RSA 是被最广泛使用的一种非对称加密算法. RSA 是 1977 年由 Ronald Linn Rivest、Adi Shamir、Leonard Max Adleman 三个人一起提出, RSA 就是他们三个人的姓氏首字母拼在一起组成的 RSA 跟对称加密比较, 更加复杂, 且更加消耗资源. 但是相对来说它的灵活性也更高; 到目前为止, 世界上还没有任何可靠的能够攻击 RSA 算法的方式. 只要其密钥长度足够长, 用 RSA 加密的数据实际上是不能破解的 ASN.1在电信和计算机网络领域, ASN.1 (Abstract Syntax Notation One) 是一套标准, 是描述数据的表示、编码、传输、解码的灵活的记法. 它提供了一套正式、无歧义和精确的规则以描述独立于特定计算机硬件的对象结构. 标准的 ASN.1 编码规则有: 基本编码规则 (Basic Encoding Rules) 简称 BER、规范编码规则 (Canonical Encoding Rules) 简称 CER、唯一编码规则 (Distinguished Encoding Rules) 简称 DER、压缩编码规则 (Packed Encoding Rules) 简称 PER 和 XML编码规则 (XML Encoding Rules) 简称 XER. 参考资料 X.509 数字证书标准X.509 是密码学里公钥证书的格式标准. X.509 证书已应用在包括 TLS/SSL 在内的众多网络协议里, 同时它也用在很多非在线应用场景里, 比如电子签名服务. X.509 证书里含有公钥、身份信息 (比如网络主机名，组织的名称或个体名称等) 和签名信息 (可以是证书签发机构CA的签名，也可以是自签名). 对于一份经由可信的证书签发机构签名或者可以通过其它方式验证的证书, 证书的拥有者就可以用证书及相应的私钥来创建安全的通信, 对文档进行数字签名. X.509 证书的结构是由 ASN.1 进行描述, 并使用 ASN.1 DER 编码规则编码 golang 实现 RSA 密钥对生成1234567891011121314151617181920212223242526272829303132333435363738394041424344454647package mainimport ( "crypto/rand" "crypto/rsa" "crypto/x509" "encoding/pem" "fmt" "os")func generatePrinAndPubKeys(size int) &#123; pri, err := rsa.GenerateKey(rand.Reader, size) if err != nil &#123; panic(fmt.Sprintf("generate rsa keys fail: %s", err)) &#125; derTx := x509.MarshalPKCS1PrivateKey(pri) block := pem.Block&#123;Type: "RSA PRIVATE KEY", Bytes: derTx&#125; file, err := os.Create("private.pem") if err != nil &#123; panic(fmt.Sprintf("create file fail: %s", err)) &#125; defer file.Close() if err := pem.Encode(file, &amp;block); err != nil &#123; panic(fmt.Sprintf("private key input file fail: %s", err)) &#125; derstream, err := x509.MarshalPKIXPublicKey(&amp;pri.PublicKey) if err != nil &#123; panic(fmt.Sprintf("marshal public key fail: %s", err)) &#125; block = pem.Block&#123;Type: "RSA PUBLIC KEY", Bytes: derstream&#125; pubFile, err := os.Create("public.pem") if err != nil &#123; panic(fmt.Sprintf("create file fail: %s", err)) &#125; defer pubFile.Close() if err := pem.Encode(pubFile, &amp;block); err != nil &#123; panic(fmt.Sprintf("private key input file fail: %s", err)) &#125;&#125;func main() &#123; generatePrinAndPubKeys(1024)&#125; golang 实现 rsa 加解密1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556package mainimport ( "crypto/rand" "crypto/rsa" "crypto/x509" "encoding/base64" "encoding/pem" "fmt" "io/ioutil")func publicKeyEncrypt(plainText []byte, keyFile string) []byte &#123; content, err := ioutil.ReadFile(keyFile) if err != nil &#123; panic(fmt.Sprintf("read public key file fail: %s", err)) &#125; block, _ := pem.Decode(content) pubInterface, err := x509.ParsePKIXPublicKey(block.Bytes) if err != nil &#123; panic(fmt.Sprintf("parse public key fail: %s", err)) &#125; pubKey := pubInterface.(*rsa.PublicKey) cipherTxt, err := rsa.EncryptPKCS1v15(rand.Reader, pubKey, plainText) if err != nil &#123; panic(fmt.Sprintf("encrypt plain fail: %s", err)) &#125; return cipherTxt&#125;func privateKeyDecrypt(cipherTxt []byte, keyFile string) []byte &#123; content, err := ioutil.ReadFile(keyFile) if err != nil &#123; panic(fmt.Sprintf("read private key file fail: %s", err)) &#125; block, _ := pem.Decode(content) priKey, err := x509.ParsePKCS1PrivateKey(block.Bytes) if err != nil &#123; panic(fmt.Sprintf("parse private key fail: %s", err)) &#125; plainTxt, err := rsa.DecryptPKCS1v15(rand.Reader, priKey, cipherTxt) if err != nil &#123; panic(fmt.Sprintf("decrypt cipher fail: %s", err)) &#125; return plainTxt&#125;func main() &#123; txt := "this is testing text" cipher := publicKeyEncrypt([]byte(txt), "public.pem") fmt.Printf("%v\n", base64.StdEncoding.EncodeToString(cipher)) fmt.Printf("%s\n", string(privateKeyDecrypt(cipher, "private.pem")))&#125; golang 实现 rsa 数字签名和校验12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364package mainimport ( "crypto" "crypto/rand" "crypto/rsa" "crypto/sha256" "crypto/x509" "encoding/pem" "fmt" "io/ioutil")func signByRsa(priFile string, plaintext []byte) []byte &#123; content, err := ioutil.ReadFile(priFile) if err != nil &#123; panic(fmt.Sprintf("read private key file fail: %s", err)) &#125; derTxt, _ := pem.Decode(content) priKey, err := x509.ParsePKCS1PrivateKey(derTxt.Bytes) if err != nil &#123; panic(fmt.Sprintf("parse private key fail: %s", err)) &#125; h := sha256.New() h.Write(plaintext) hashed, err := rsa.SignPKCS1v15(rand.Reader, priKey, crypto.SHA256, h.Sum(nil)) if err != nil &#123; panic(fmt.Sprintf("sign plaintext fail: %s", err)) &#125; return hashed&#125;func verifyByRsa(pubFile string, plaintext, sign []byte) bool &#123; content, err := ioutil.ReadFile(pubFile) if err != nil &#123; panic(fmt.Sprintf("read public key file fail: %s", err)) &#125; derTxt, _ := pem.Decode(content) pubInterface, err := x509.ParsePKIXPublicKey(derTxt.Bytes) if err != nil &#123; panic(fmt.Sprintf("parse public key fail: %s", err)) &#125; pubKey := pubInterface.(*rsa.PublicKey) h := sha256.New() h.Write(plaintext) if err := rsa.VerifyPKCS1v15(pubKey, crypto.SHA256, h.Sum(nil), sign); err != nil &#123; return false &#125; return true&#125;func main() &#123; txt := []byte("this is testing text") sign := signByRsa("private.pem", txt) fmt.Printf("sign verify result: %v\n", verifyByRsa("public.pem", txt, sign))&#125; 在 golang 中我没有找到可以使用 私钥 加密, 公钥 解密的接口. 可能需要自己实现 参考和引用公钥密码学标准X.509ASN.1RSA加密算法公开密钥加密]]></content>
      <tags>
        <tag>golang</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[DES/AES 对称加密]]></title>
    <url>%2Fposts%2FDES%2FAES%20%E5%AF%B9%E7%A7%B0%E5%8A%A0%E5%AF%86.html</url>
    <content type="text"><![CDATA[对称加密 (Symmetric-key algorithm) 又被称为: 对称秘钥加密、私钥加密、共享密钥加密. 是密码学中一个种加密算法, 这类加密算法在加密和解密时需要使用到同一个秘钥. 根据不同的加密算法秘钥长度也不同, 一般在 64bit ~ 256bit 之间, 具体长度取决于所需的安全级别, 但是秘钥长度越长加密和解密所需要消耗的资源越多. 常用的对称加密算法有: DES (Data Encrytion Standard)、3DES (Triple DES)、AES (Advanced Encryption Standard) 等; DES 加密Data Encrytion Standard 简称 DES, DES 是一种分组加密算法, 以 64bit 为单位将明文分割并加密. DES 的秘钥长度也是 64bit (8字节), 每个字节中 1bit-7bit 为数据位, 第 8bit 为奇偶校验位, 也就是说秘钥中: 8bit、16bit、24bit、32bit、40bit、48bit、56bit、64bit 都是校验位, 所以也有 DES 的秘钥长度为 56bit 的说法. 美国国家标准局于 1977 年公布把它作为非机要部门使用的数据加密标准，三十年来，它一直活跃在国际保密通信的舞台上，扮演了十分重要的角色. 但随着计算机的进步, DES 已经能够被暴力破解. 3DES 加密三重 DES 加密 (Triple DES) 简称 3DES. 现在 DES 已经可以暴力破解, 所以需要一种分组加密算法来代替 DES, 而 3DES 由此被开发出来代替 DES. 3DES 可以看做是 DES 的升级版. 3DES 的秘钥可分为三部分: K1、K2、K3, 三个秘钥决定了加密的安全性. 若数据安全性要求不那么高, K1 可以等于 K3, 也可以 K1、K2、K3 都使用同一秘钥 (相当于 DES 加密, 多此一举还耗费资源) AES 加密高级加密标准 (Advanced Encryption Standard) 简称 AES, 在 DES 可被暴力破解不再安全之后, AES 能够抵御已知的针对 DES 算法的所有攻击, 且比 3DES 加密效率更高, 安全性也更高 AES 也是分组加密算法, AES 的区块长度固定为 128bit, 秘钥长度则可以为 128bit、192bit或256bit (golang 中只支持 128bit 和 192bit 长度的秘钥); 加密过程中使用的密钥是由 Rijndael 密钥生成方案 产生. 加密模式密码学中, 分组（block）密码的工作模式（mode of operation）允许使用同一个分组密码密钥对多于一块的数据进行加密, 并保证其安全性. 分组密码自身只能加密长度等于密码分组长度的单块数据, 若要加密变长数据, 则数据必须先被划分为一些单独的密码块. 通常而言, 最后一块数据也需要使用合适填充方式将数据扩展到符合密码块大小的长度. 一种工作模式描述了加密每一数据块的过程, 并常常使用基于一个通常称为初始化向量的附加输入值以进行随机化, 以保证安全. 所有加密模式图示和代码示例只提供加密过程, 如需解密过程请根据加密过程反推 ECB 模式电子密码本模式 (Electronic codebook) 简称 ECB, 需要加密的消息按照块密码的块大小被分为数个块, 并对每个块进行独立加密. 这样的加密方式会让密文有规律, 安全性较低, 常用在对数据安全性要求不高的场景. 加密过程图示: DES ECB模式加密和解密实现12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061package mainimport ( "bytes" "crypto/des" "fmt")// The CBC mode and the ECB mode, there are strict limits on the size of the groups,// During encryption, if the last group of the plaintext does not meet the group size,// You need to fill in the group size// By convention, the value of the supplementary array to be filled// should be (block size - current size).// If the clear text is just big enough to be grouped,// then by convention you should also populate a whole set of data with a value of block sizefunc paddingLastGroup(plaintext []byte, blockSize int) []byte &#123; padNum := blockSize - len(plaintext)%blockSize char := []byte&#123;byte(padNum)&#125; newPlain := bytes.Repeat(char, padNum) return append(plaintext, newPlain...)&#125;// According to the previous fill specification,// the fill data in the plaintext after decryption needs to be removedfunc unPaddingLastGroup(plaintext []byte) []byte &#123; length := len(plaintext) return plaintext[:length - int(plaintext[length - 1])]&#125;func desEncrypt(plaintext, key []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; newPlain := paddingLastGroup(plaintext, block.BlockSize()) block.Encrypt(newPlain, newPlain) return newPlain&#125;func desDecrypt(cipherText, key []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; block.Decrypt(cipherText, cipherText) return unPaddingLastGroup(cipherText)&#125;func main() &#123; str := "this test string" key := []byte("12345678") cipherText := desEncrypt([]byte(str), key) fmt.Printf("After encryption: %v\n", cipherText) plaintext := desDecrypt(cipherText, key) fmt.Printf("After decryption: %s\n", string(plaintext))&#125; CBC 模式密码分组链接 (Cipher-block chaining) 简称 CBC, 在CBC模式中, 每个明文块先与前一个密文块进行异或后, 再进行加密. 在这种方法中, 每个密文块都依赖于它前面的所有明文块. 同时, 为了保证每条消息的唯一性, 在第一个块中需要使用初始化向量. IV 初始化向量: 初始化向量 (Initialization Vector) 是许多任务作模式中用于将加密随机化的一个位块, 由此即使同样的明文被多次加密也会产生不同的密文, 避免了较慢的重新产生密钥的过程. 初始化向量与密钥相比有不同的安全性需求, 因此IV通常无须保密, 然而在大多数情况中, 不应当在使用同一密钥的情况下两次使用同一个IV. DES CBC 模式加密和解密实现1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465package mainimport ( "bytes" "crypto/cipher" "crypto/des" "fmt")// The CBC mode and the ECB mode, there are strict limits on the size of the groups,// During encryption, if the last group of the plaintext does not meet the group size,// You need to fill in the group size// By convention, the value of the supplementary array to be filled// should be (block size - current size).// If the clear text is just big enough to be grouped,// then by convention you should also populate a whole set of data with a value of block sizefunc paddingLastGroup(plaintext []byte, blockSize int) []byte &#123; padNum := blockSize - len(plaintext)%blockSize char := []byte&#123;byte(padNum)&#125; newPlain := bytes.Repeat(char, padNum) return append(plaintext, newPlain...)&#125;// According to the previous fill specification,// the fill data in the plaintext after decryption needs to be removedfunc unPaddingLastGroup(plaintext []byte) []byte &#123; length := len(plaintext) return plaintext[:length-int(plaintext[length-1])]&#125;func desEncrypt(plaintext, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; newPlain := paddingLastGroup(plaintext, block.BlockSize()) blockMode := cipher.NewCBCEncrypter(block, iv) blockMode.CryptBlocks(newPlain, newPlain) return newPlain&#125;func desDecrypt(cipherText, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; blockMode := cipher.NewCBCDecrypter(block, iv) blockMode.CryptBlocks(cipherText, cipherText) return unPaddingLastGroup(cipherText)&#125;func main() &#123; str := "this test string" key := []byte("12345678") iv := []byte("abcdefgh") cipherText := desEncrypt([]byte(str), key, iv) fmt.Printf("After encryption: %v\n", cipherText) plaintext := desDecrypt(cipherText, key, iv) fmt.Printf("After decryption: %s\n", string(plaintext))&#125; CFB 模式密文反馈 (Cipher feedback) 简称 CFB, 类似于CBC, 可以将块密码变为自同步的流密码; 工作过程亦非常相似, CFB的解密过程几乎就是颠倒的CBC的加密过程： DES CFB 模式加密和解密实现1234567891011121314151617181920212223242526272829303132333435363738394041package mainimport ( "crypto/cipher" "crypto/des" "fmt")func desEncrypt(plaintext, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; blockMode := cipher.NewCFBEncrypter(block, iv) blockMode.XORKeyStream(plaintext, plaintext) return plaintext&#125;func desDecrypt(cipherText, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; blockMode := cipher.NewCFBDecrypter(block, iv) blockMode.XORKeyStream(cipherText, cipherText) return cipherText&#125;func main() &#123; str := "this test string" key := []byte("12345678") iv := []byte("abcdefgh") cipherText := desEncrypt([]byte(str), key, iv) fmt.Printf("After encryption: %v\n", cipherText) plaintext := desDecrypt(cipherText, key, iv) fmt.Printf("After decryption: %s\n", string(plaintext))&#125; OFB 模式输出反馈模式 (Output feedback) 简称 OFB, 可以将块密码变成同步的流密码. 它产生密钥流的块, 然后将其与明文块进行异或, 得到密文. 与其它流密码一样, 密文中一个位的翻转会使明文中同样位置的位也产生翻转. 这种特性使得许多错误校正码, 例如奇偶校验位, 即使在加密前计算, 而在加密后进行校验也可以得出正确结果. DES OFB 模式加密和解密实现1234567891011121314151617181920212223242526272829package mainimport ( "crypto/cipher" "crypto/des" "fmt")func desOfbEncryptOrDecrypt(plaintext, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; blockMode := cipher.NewOFB(block, iv) blockMode.XORKeyStream(plaintext, plaintext) return plaintext&#125;func main() &#123; str := "this test string" key := []byte("12345678") iv := []byte("abcdefgh") cipherText := desOfbEncryptOrDecrypt([]byte(str), key, iv) fmt.Printf("After encryption: %v\n", cipherText) plaintext := desOfbEncryptOrDecrypt(cipherText, key, iv) fmt.Printf("After decryption: %s\n", string(plaintext))&#125; CTR 模式计数器模式 (Counter mode) 简称 CTR (也被称为 整数计数模式 (Integer Counter Mode) 继承 ICM 或 SIC（Segmented Integer Counter）), 与 OFB 相似, CTR 将块密码变为流密码. 它通过递增一个加密计数器以产生连续的密钥流, 其中, 计数器可以是任意保证长时间不产生重复输出的函数, 但使用一个普通的计数器是最简单和最常见的做法. 注意图中的 “随机数” 与其它图中的 IV (初始化向量) 相同。IV、随机数和计数器均可以通过连接, 相加或异或使得相同明文产生不同的密文 DES CTR 模式加密和解密实现1234567891011121314151617181920212223242526272829package mainimport ( "crypto/cipher" "crypto/des" "fmt")func desCtrEncryptOrDecrypt(plaintext, key, iv []byte) []byte &#123; block, err := des.NewCipher(key) if err != nil &#123; panic(err) &#125; blockMode := cipher.NewCTR(block, iv) blockMode.XORKeyStream(plaintext, plaintext) return plaintext&#125;func main() &#123; str := "this test string" key := []byte("12345678") iv := []byte("abcdefgh") cipherText := desCtrEncryptOrDecrypt([]byte(str), key, iv) fmt.Printf("After encryption: %v\n", cipherText) plaintext := desCtrEncryptOrDecrypt(cipherText, key, iv) fmt.Printf("After decryption: %s\n", string(plaintext))&#125; 参考数据加密标准高级加密标准分组密码工作模式]]></content>
      <tags>
        <tag>golang</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Golang 进程热重启]]></title>
    <url>%2Fposts%2Fgolang-hot-reload.html</url>
    <content type="text"><![CDATA[There are only 10 kinds of people in the world, one is a person who understands binary, and the other is a person who does not understand binary. 代码需要升级; 配置文件需要更新. 如果在 PHP 中这些都很简单, 一个简单的 vim 或 git pull 就能解决. 可是像 Golang 这样的静态语言就无法做到像 PHP 中的那么灵活. 在线上的生产环境, 我们必须保证新老服务无缝交替. 如果贸然停掉服务, 就可能造成一些问题; 例如: 旧的请求未完成, 服务端进程直接退出, 会造成客户端链接中断 新的请求发送过来, 服务端还未完成重启, 造成 connection refused 旧的请求未处理完成, 服务端进程直接退出, 可能会造成用户数据不对称(即: 用户已完成一部分操作, 还要一部分未完成) 当然; 这些问题都可以通过其他方法去规避. 例如使用负载均衡(如: nginx), 保证在升级的过程中始终有一个服务可用, 即各服务器依次灰度升级. 但是像 nginx -s reload 这种的方式, 好像更加方便 什么是 热启动热启动也可以叫平滑启动, 其思想就是新老程序 (进程) 无缝交替, 一直保持对客户端的服务, 让客户端感觉不到服务的重启. 且新程序启动之后所有新的请求会直接打到新程序, 老程序处理完旧请求之后(或超时)安全(或超时)退出. 其实现原理就是拦截 Linux 的信号, 例如 nginx -s reload 就是给 nginx 发送一个表示 “注意: 我需要重启啦” 这样的信号. 当 nginx 接收到这样的信号的时候, 就会 fork 一个子进程, 并将父进程的 socket 句柄交给子进程 ,将新的请求打到子进程中, 父进程在处理完旧进程之后安全退出. 父进程安全退出之后, 子进程会被 Linux 的 init 进程领养成为新的 nginx 进程. 如此循环 Linux/Unix 下三个特殊进程和孤儿进程在编写代码之前, 我们先来了解一下 Linux/Unix 下三个特殊的进程 idle、init、kthreadd 以及什么是 孤儿进程 idle 进程: PID 为 0, 其前身是系统创建的第一个进程, 也是 唯一 一个不是被 fork 或 kernel_thread 创建的进程; 完成加载系统后, 演变为进程调度, 交换 init 进程: PID 为 1, 由 idle 进程通过 kernel_thread 创建. 在内核空间初始化完成以后, 加载 init 进程. 在 linux 中所有进程都是由 init 进程创建并运行(PS: PID 为 0、1、2 的除外)的 kthreadd 进程: PID 为 2, 也是由 idel 进程通过 kernel_thread 创建. kthreadd 进程始终运行在内核中, 它的任务就是管理和调度其他内核线程 kernel_thread, 它会 循环 执行一个 kthread 函数, 该函数的作用就是运行 kthread_create_list 全局链表中的 kthread. 当我们调用 kernel_therad 创建内核线程时, 该线程会被加入到 kthread_create_list 链表中, 所以所有的内核线程都是直接或间接的以 kthreadd 为父进程 孤儿进程 : 除 idle 进程以外的所有进程都有一个父进程(init 和 kthreadd 的父进程就是 idle), 但是进程是可以被 杀死 的.当一个进程退出, 而它有一个或多个进程还在运行, 那么这些子进程就会因为父进程被 Kill 掉变成没有父进程的进程, 这种进程我们就称为孤儿进程. 但是孤儿进程显然不符合 Linux/Unix 的进程规范. 所以在父进程表 Kill 掉以后, init 会领养这样的孤儿进程, 由 init 作为它们的父进程. 演示 孤儿进程 被 Init 收养 在 Unix 中, 创建进程是通过系统调用 fork 实现, 在 GO 语言中的 Linux 下创建进程使用的系统调用是 clone. 子进程几近于父进程的翻版, 子进程获得父进程的栈、数据段、堆和执行文本段的拷贝. 可以视为把父进程一分为二 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687package mainimport ( "flag" "fmt" "log" "net" "net/http" "os" "os/exec" "syscall" "time")type Handler struct&#123;&#125;func (handler *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request) &#123; fmt.Println("Request start at", time.Now(), r.URL.Path+"?"+r.URL.RawQuery, "request done at", time.Now(), " pid:", os.Getpid()) time.Sleep(5 * time.Second) _, err := w.Write([]byte("this is current request response")) if err != nil &#123; fmt.Println("Current request failure:", err.Error()) &#125; fmt.Println("Request done at", time.Now(), " pid:", os.Getpid())&#125;func main() &#123; var ( err error listener net.Listener // Pass the parameter on the command line to decide whether to fork a child process // If there is no parameter control, the child process will fork another process, // and the fork process will fork a child process. // This has been the fork child process, obviously this is not what we need fork = flag.Bool("f", false, "Parameter determines whether to fork a child process") ) flag.Parse() if *fork &#123; listener, err = net.Listen("tcp", ":10005") if err != nil &#123; panic("Listen port 10005 failure: " + err.Error()) &#125; tcp, _ := listener.(*net.TCPListener) fd, _ := tcp.File() fmt.Printf("Current process fd: %v, filename: %v, %#v\n", fd.Fd(), fd.Name(), listener) &#125; else &#123; f := os.NewFile(3, "") listener, err = net.FileListener(f) if err != nil &#123; panic(fmt.Sprintf("Listen file %s failure fd: %v error: %s\n", f.Name(), f.Fd(), err.Error())) &#125; fmt.Printf("Child process fd: %v, filemame: %v, %#v\n", f.Fd(), f.Name(), listener) &#125; server := http.Server&#123;Handler: &amp;Handler&#123;&#125;, ReadTimeout: 6 * time.Second&#125; log.Println("Actual pid is ", syscall.Getpid()) log.Printf("Listener: %v \n", listener) if *fork &#123; // To make it easier to check the status of the process, fork child process after 10 seconds timer := time.NewTimer(10 * time.Second) go func() &#123; &lt;- timer.C tl, _ := listener.(*net.TCPListener) currentFd, err := tl.File() if err != nil &#123; panic("acquiring listener file failed") &#125; // Here, you need to remove the parent process's -f parameter to prevent the child process // from creating the child process. cmd := exec.Command(os.Args[0]) cmd.ExtraFiles, cmd.Stdout, cmd.Stderr = []*os.File&#123;currentFd&#125;, os.Stdout, os.Stderr err = cmd.Start() if err != nil &#123; panic(fmt.Sprintf("cmd.Start fail:%s", err)) &#125; fmt.Println("forked new pid: ", cmd.Process.Pid) &#125;() &#125; err = server.Serve(listener) if err != nil &#123; log.Println(err) &#125;&#125; 通过 go build main.go 编译后获得一个 mian 命令, 我们先来看一下在没有 fork 子进程 之前的请求状态(加上 -f 参数): 服务启动后我通过 curl http://127.0.0.1:10005 发送了几个请求, 请求正常输出. 10秒之后 子进程 被 fork 出来之后 (PS:父进程还没有被杀掉) 我又发送了几个请求如下: 通过上面的图片我们可以看出, 两个进程都是在运行的且监听的是同一个端口, 两个进程都在请求. 通过 ps -ef 命令查看它们的进程状态如下: 在父进程还没有被杀死的时候, 子进程的 PPID 为父进程ID, 正常! 接下来 我们通过命令 kill 4555 杀死父进程, 查看子进程状态: 当父进程 4555 被杀死后, 子进程的 PPID 变成了 1. 跟我们上述的理论没有差别, 我们在通过 curl 尝试请求端口 10005 看是否存在差异: os.NewFile(3, “”) 解析: os.NewFile 接收两个值, 一个为文件描述符(FD), 一个是文件名, 该函数并不是创建一个新的文件, 而是新建一个文件但是不保存返回文件指针. 在 Linux 系统中一切都可以看成是文件, 文件可分为: 普通文件、目录文件、链接文件和设备文件. 文件描述符 (file descriptor) 是内核为了高效管理已打开文件所创建的索引, 为一个 非负整数 (通常是一个小整数), 用于指代被打开的文件, 所有执行 I/O 操作的系统调用都需要用到文件描述符. 程序刚启动时: 0 为标准输入(stdio)、1 为标准输出(stdout)、2 是标准错误(stderr). 如果此时需要打开一个新文件, 那么它的描述符就是 3 (这里的打开包括创建). POSIX标准要求每次打开文件时（含socket）必须使用当前进程中最小可用的文件描述符号码，因此，在网络通信过程中稍不注意就有可能造成串话 实现热启动通过上面的示例我们发现当父进程被杀死后, 子进程会被 Init 进程收养称为新的主进程, 那么我们就通过这种逻辑完成对新老服务的交替, 在子进程启动后且父进程处理完旧请求后安全退出父进程 (PS: 上述示例中, 在子进程启动后并没有关闭父进程的请求接收, 所有父进程和子进程都在处理请求, 在热启动中我们会关闭父进程请求接收). 代码实现修改代码如下: 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116117118package mainimport ( "context" "flag" "fmt" "log" "net" "net/http" "os" "os/exec" "os/signal" "sync" "syscall" "time")type Handler struct&#123;&#125;func (handler *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request) &#123; fmt.Println("Request start at", time.Now(), r.URL.Path+"?"+r.URL.RawQuery, "request done at", time.Now(), " pid:", os.Getpid()) time.Sleep(5 * time.Second) _, err := w.Write([]byte("this is current request response")) if err != nil &#123; fmt.Println("Current request failure:", err.Error()) &#125; fmt.Println("Request done at", time.Now(), " pid:", os.Getpid())&#125;func main() &#123; var ( err error listener net.Listener // You need a wait group to wait for the current process (parent process) http service to shut down gracefully, // otherwise, after calling server.Shutdown, the main thread will stop blocking the entire process exit, // and the old requests that are not completed will be discarded // And this wait group must be a pointer (the data exists on the heap), // because we said before: The child process is the stack area of the shared parent process, // when the wait group exists on the stack, it will cause two processes to call a same one wait group group = new(sync.WaitGroup) graceful = flag.Bool("g", false, "When the service is started for the first time, "+ "there is no need to perform hot restart related operations") ) flag.Parse() if *graceful &#123; f := os.NewFile(3, "") listener, err = net.FileListener(f) if err != nil &#123; panic(fmt.Sprintf("Listen file %s failure fd: %v error: %s\n", f.Name(), f.Fd(), err.Error())) &#125; fmt.Printf("Child process fd: %v, filemame: %v, %#v\n", f.Fd(), f.Name(), listener) &#125; else &#123; listener, err = net.Listen("tcp", ":10005") if err != nil &#123; panic("Listen port 10005 failure: " + err.Error()) &#125; tcp, _ := listener.(*net.TCPListener) fd, _ := tcp.File() fmt.Printf("Current process fd: %v, filename: %v, %#v\n", fd.Fd(), fd.Name(), listener) &#125; server := http.Server&#123;Handler: &amp;Handler&#123;&#125;, ReadTimeout: 6 * time.Second&#125; log.Println("Actual pid is ", syscall.Getpid()) log.Printf("Listener: %v \n", listener) group.Add(1) go func(group *sync.WaitGroup) &#123; defer group.Done() signalChan := make(chan os.Signal, 1) signal.Notify(signalChan, syscall.SIGHUP, syscall.SIGTERM) ListenSignal: for &#123; sig := &lt;-signalChan // Give the http service that needs to be gracefully shut down a timeout to // prevent the parent process from persisting ctx, _ := context.WithTimeout(context.Background(), 20*time.Second) switch sig &#123; case syscall.SIGTERM, syscall.SIGHUP: tl, _ := listener.(*net.TCPListener) currentFd, err := tl.File() if err != nil &#123; panic("acquiring listener file failed") &#125; cmd := exec.Command(os.Args[0], "-g") cmd.ExtraFiles, cmd.Stdout, cmd.Stderr = []*os.File&#123;currentFd&#125;, os.Stdout, os.Stderr err = cmd.Start() if err != nil &#123; panic(fmt.Sprintf("cmd.Start fail:%s", err)) &#125; fmt.Println("forked new pid: ", cmd.Process.Pid) // When the child process fork is complete, close the http service of the parent process, // so the parent process will not receive new requests. err = server.Shutdown(ctx) if err != nil &#123; fmt.Println("shutdown fail: ", err) &#125; // After closing the parent process http service, // you need to jump out of the loop and stop monitoring the signal, // otherwise the parent process will still exist break ListenSignal &#125; &#125; &#125;(group) err = server.Serve(listener) if err != nil &#123; log.Println(err) &#125; fmt.Printf("Pid %d http server closed\n", os.Getpid()) // After calling server.Shutdown, server.Serve will stop blocking the main thread, // so here we need to wait for the old request to be processed or timed out group.Wait()&#125; 测试结果我通过 ab 测试, 发送了 100 个测试请求, 并在中间关闭了父进程, 结果如下: 测试结果 OK, 没有请求失败的情况. 数据来源Golang服务器热重启、热升级、热更新详解(有BUG, 主线程会一直监听信号, 不会退出)Linux下1号进程的前世(kernel_init)今生(init进程)——Linux进程的管理与调度]]></content>
      <tags>
        <tag>golang</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[使用阿里云 API 申请 Let’s Encrypt 泛域名免费 SSL 证书]]></title>
    <url>%2Fposts%2Fconfigure-nginx-https-certificate.html</url>
    <content type="text"><![CDATA[The Internet has made the world today, and today’s world has made the Internet 最近帮朋友写了个小程序, 写完啦, 要测试啦. 没有 https 证书!! 我个人比较喜欢把测试代码放到自己的服务器上, 这样方便我自己修改 (PS: 自己的服务器嘛, 想怎么来怎么来). 所以得想办法整个 https 证书. 本来想看看网上有没有免费的, 一个不小心就 Google 到阿里云就有免费的证书. 刚好服务器是阿里的, 域名也是阿里的. 所以决定整一个用用 网站部署 HTTPS 的重要性互联网造就了很多产业, 也造就了很多黑产业. 让我们想象一下: 当我们访问淘宝、京东的时候. 右下角漂浮着 “大家好我是渣渣辉, 是兄滴就来啃我”. 这种感觉肯定特别的酸爽 (PS: 哈哈哈哈哈, 要真是这样张家辉肯定还会火点). 或者我们再来想象一下: 当我们输入银行的网址后打算转账的时候, 卡号输了、密码输了、钱没到账不说, 银行卡里一分不剩, 这是不是就很悲哀了呢 上举的两个例子其实是真实存在的. 在输入网址访问网页, 到网页返回数据给我们的过程中, 其实是分很多步骤的比如最基础的 DNS 解析 (PS: 不做详解). 如果在 DNS 解析的过程中被人劫持并注入一些东西, 就可能会出现上述的两种情况 HTTP 和 HTTPS 的区别HTTP 协议或者说 超文本传输协议 是通过明文的方式发送内容的, 不提供任何方式的数据加密, 如果有攻击者截取到了浏览器和服务器之间的传输报文, 就可以直接读懂其中的信息, 这对我们一些如: 住址、卡号、密码等敏感数据来说是非常不安全的. 为了解决这一个缺陷, 就需要使用另一种协议 HTTPS 安全套接字层超文本传输协议, 为了数据传输安全, HTTPS 在 HTTP 的基础上加入了 SSL 协议, SSL 依靠证书来验证服务器身份, 并为浏览器和服务器之间传递的数据加密 相对于 HTTP 来说, HTTPS 更加安全、更加可靠, 谷歌 曾明确表示: “希望所有站长都是使用 HTTPS, 而非 HTTP“; 其搜索引擎在算法更新中也表示: “同等条件下，使用HTTPS加密技术的站点在搜索排名上更具优势”. 这两点可以看出其对 HTTPS everywhere 这一目标的决心 使用阿里云 API 申请 Let’s Encrypt 泛域名免费 SSL 证书SSL 证书是部署 HTTPS 的必要条件, 由受信任的数字证书颁发机构 CA，在验证服务器身份后颁发. Let&#39;s Encrypt 是一个个免费、自动化、开放的证书签发服务。它由 ISRG（Internet Security Research Group，互联网安全研究小组）提供服务，而 ISRG 是来自于美国加利福尼亚州的一个公益组织。Let&#39;s Encrypt 得到了 Mozilla、Cisco、Akamai、Electronic Frontier Foundation 和 Chrome 等众多公司和机构的支持. 安装依赖和申请工具1sudo apt-get update &amp;&amp; sudo apt-get install curl cron socat -y 1curl https://get.acme.sh | sh 导入阿里的 Key 和 Secret使用 阿里云 的 API 申请 Let&#39;s Encrypt 需要提供阿里云的 Key 和 Secret [传送门])(https://usercenter.console.aliyun.com/#/manage/ak) 直接登录就可以申请, 获取到 Key 和 Secret 后导入到环境变量中 12export Ali_Key=&quot;xxxxx&quot;export Ali_Secret=&quot;xxxxx&quot; 申请证书和设置定时重新申请1~/.acme.sh/acme.sh --issue --dns dns_ali -d domain.com -d &apos;*.domain.com&apos; 出现以下图片内容就表示成功了 通过 Let&#39;s Encrypt 申请的 SSL 证书有效时间只有 90 天, 所以到时间后需要重新申请一次, 但是这样太麻烦了. 不过 acme.sh 脚本已经帮我们做好了这一步, 只需要执行一个命令就可以了: 1/home/ubuntu/.acme.sh/acme.sh --install-cert -d domain.com --key-file /home/ubuntu/ssl/domain.com/domain.com.key --fullchain-file /home/ubuntu/ssl/domain.com/domain.com.cer --reloadcmd &quot;sudo service nginx force-reload&quot; 成功之后执行 crontab -l 可以看到有一个定时任务, 证书到期会自动续签 参考资料使用阿里云域名api申请Let’s Encrypt泛域名免费ssl证书]]></content>
      <tags>
        <tag>Nginx</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[VPS + SSR 实现科学上网]]></title>
    <url>%2Fposts%2Fscience-online.html</url>
    <content type="text"><![CDATA[今天公司的梯子挂掉了我很伤心! 对于我这种不知道是几流的程序员不能 Google 实在是太难受了, 本来自己有一个付费的梯子奈何公司的速度快 (PS:白嫖真的太爽了). 靠人不如靠自己,所以我决定自己搭个梯子自己用,谁都不给! (PS:不能让我对象知道). 真诚表态: 梯子只是为了更好的学习, 不做不利于国家的事情 选择 VPSVPS (Virtual Private Server 虚拟专用服务器), 说白了要搭梯子得先有个能访问国外资源的服务器不是, VPS 有很多选择: AWS、Vultr、搬瓦工 等.我这边直接选的是 Vultr, Vultr 是按小时收费的, 因为现在管控的比较严所以随时都有可能被强掉, 按小时计费的好处就是可以随时销毁随时创建. 当然从价格上来看 搬瓦工 是最便宜的, 大家可以根据自己的情况选择 VPS. 在我购买截止为止 Vultr 的服务器最低是 5 刀每个月 (PS: 一年也就三四百块钱, 我个人觉得还可以). 购买 Vultr VPS 选服务器系统是最好选 CentOs 6 因为 7 的防火墙会对 SSR 造成一些问题 选择好服务器后一定要先 Ping 一下, 如果 ping 不通的话, 先不要销毁服务器. 重新开一台服务器 (这样 IP 是会变的, 如果先销毁了服务器, IP 会回到 IP 池中, 这样下次开出来的服务器 IP一样), 直到 Ping 通为止 部署 VPS 服务器安装 ShadowsocksR 服务器端1wget --no-check-certificate https://freed.ga/github/shadowsocksR.sh; bash shadowsocksR.sh 如果出现以下图片, 恭喜你安装成功了, 请妥善保存图片内的内容 (PS: 因为我这边安装的过程中没有遇到问题, 所以如果遇到问题的话请自行 Google, 后续如果我遇到问题也会补充) 如果是一个非技术出生的话你可以查一下怎么登陆 VPS 服务器, 我这边就不做详解了 安装 Google BBR 进行加速因为 Vultr 的所有机房都位于国外，当晚上上网高峰期来临时，在连接速度上会比较慢，所以我们有必要安装一些程序来加速连接速度。个人认为目前在提速方面比较中意 Google BBR, 但是 Google BBR 相对于 锐速 在速度方面还有些不足, 所以我上的是 魔改版 (PS: 魔改大发好呀). 因为我这边的系统选择的是 CentOS6 所以直接演示 CentOS6 的安装步骤, 如果你是其他系统或其他版本的话, 可以在网上查一下安装方式 (PS: 有很多很多) 1wget --no-check-certificate https://raw.githubusercontent.com/nanqinlang-tcp/tcp_nanqinlang/master/General/CentOS/bash/tcp_nanqinlang-1.3.2.sh &amp;&amp; bash tcp_nanqinlang-1.3.2.sh 出现图片内容后 输入数字 1, 之后的选项直接 输入 y 直到安装成功后提示 “请确认此行上面的列表显示的内核版本后，重启以应用新内核”. 注意: 不要自己重启, 通过 Vultr 后台重启服务器! 不要自己重启, 通过 Vultr 后台重启服务器! 不要自己重启, 通过 Vultr 后台重启服务器! 重要的事情说三遍! 重启服务器后运行 bash tcp_nanqinlang-1.3.2.sh 并 输入数字 2, 这样就安装成功啦~~~ 参考资料 用VPS搭建SSR服务器教程]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hexo 搭配 Github pages 搭建个人博客]]></title>
    <url>%2Fposts%2Fhexo-install.html</url>
    <content type="text"><![CDATA[作为一个 IT 行业的菜鸟，有的时候在网上浏览到一些好的技术文章，看过之后转头就忘记了，所以有一个自己的技术博客做一些记录是非常有必要的。但是个人博客虽然简单但是开发起来费时费力，而且如果要添加什么功能也非常的麻烦；SO 选一个开源的博客工具是一个非常好的办法。这里就记录一下使用 Hexo 和 Github pages 搭建个人博客的步骤、插件的集成和碰到的问题 那么为什么要使用 Github pages 来搭建个人博客呢？ 支持绑定自己的域名 提交网站等同于上传自己的代码 提供免费的空间，而且就在自己的仓库里 (PS: 在服务器上维护自己的 Blog 真的很麻烦!!) 安装 node 和 npmHexo 是使用 nodejs 开发的，所有如果要使用 Hexo 的话首先要安装 nodejs 和 npm (PS: git 也是必须要的，但是这个我就不记录了，不然太 low! 哈哈哈哈哈)，至少安装 6.9 版本以上的 node。下面记录一些 Mac 系统使用 Homebrew 安装 node 和 npm 的过程；如果是 windows 用户的话可以参照 廖雪峰的博客 中的 安装Node.js和npm Homebrew 安装 安装 1$ brew install node &amp;&amp; brew install npm 查看结果 安装 Hexo 安装 hexo-cli 命令行工具: npm install hexo-cli -g 创建工作目录并初始化 hexo: mkdir hexo &amp;&amp; cd hexo &amp;&amp; hexo init 运行 npm: npm install 经过上面步骤基本上博客的基本框架就已经搭建好了，这个时候我们可以先在本地运行一下看一下效果；运行 hexo g &amp;&amp; hexo s (PS: 关于 Hexo 的命令可以查看 Hexo官方文档) 修改站点配置文档网站基础配置，修改站点配置文件 _config.yml12345678# Sitetitle: Hexo + Github page 搭建个人博客示例subtitle: Hexo + Github page 搭建个人博客示例副标题description: 网站 descriptionkeywords: 网站 keywords 多个关键词英文逗号分隔author: Reliylanguage: zh-CNtimezone: Asia/Shanghai Git 仓库配置使用 git 作为提交方式需要先安装插件 npm install hexo-deployer-git –save 123456# Deployment# Docs: https://hexo.io/docs/deployment.htmldeploy: type: git repo: git@github.com:xxx/xxx.github.io.git # 对应的 git 仓库地址 branch: master 安装 Next 主题Hexo 在我看来最大的特点 (PS: 也是我选择它的原因) 就是可以根据自己的喜好来选择主题和集成插件。在 Github 上有很多别人写好的主题可以拿来用；包括官方也提供了不少主题可以选择: Hexo官方主题；当然如果有那个能力的话自己写一套主题也是不错的选择。经过比较之后我选择使用 Next 主题，因为该主题集成了不少插件基本上可能需要使用到的都会有；而且主题样式也不错。这里我只写基本步骤，具体的 Next 主题配置可以查看 Next官方文档 安装主题1$ git clone https://github.com/theme-next/hexo-theme-next themes/next 修改站点配置文件 _config.yml 中内容1234# Extensions# Plugins: https://hexo.io/plugins/# Themes: https://hexo.io/themes/theme: next 修改 Next 主题的页面风格, 编辑主题配置文件 themes/next/_config.yml, Next 提供了 4 中页面风格可以使用，可以根据自己的喜好选择12345# Schemes#scheme: Musescheme: Mist#scheme: Pisces#scheme: Gemini 开启本地搜索插件安装 hexo-generator-searchdb，在站点的根目录下执行以下命令：1$ npm install hexo-generator-searchdb --save 编辑站点配置文件 _config.yml, 添加以下内容到任意位置123456# Local search plug-in configurationsearch: path: search.xml field: post format: html limit: 10000 修改主题文件 themes/next/_config.yml123# Local searchlocal_search: enable: true 效果 给博客添加 Gitalk 评论插件Next 主题原生支持几种评论插件，其中包括 disqus 和 gitment (PS: 个人觉得这两个比较好用)；但是 disqus 对国内的网络支持非常的不好 (PS: 国内嘛很正常)，所以我原本选择的是 gitment，集成好 gitment 之后发现用不了；包括现在集成了 gitment 的博客好像都出了问题。在解决的过程中发现了 Gitalk，基本和 gitment 一样 申请 Github applicationGitalk 是一个利用 Github Api, 基于 Github issue 和 Preact 开发的评论插件；由于需要调用 Github Api 需要先申请一个 Github Appliction 用于认证登录。 申请链接 点击注册之后，跳转到如下页面；其中的 Client ID 和 Client Secret 是需要使用到的。 在 Next 中集成 Gitalk因为 Next 主题并不是原生支持 Gitalk 的，所以需要自己手动的集成到主题中。 新建 themes/next/layout/_third-party/comments/gitalk.swig 文件，并添加内1234567891011121314151617&#123;% if page.comments &amp;&amp; theme.gitalk.enable %&#125; &lt;link rel="stylesheet" href="https://unpkg.com/gitalk/dist/gitalk.css"&gt; &lt;script src="https://unpkg.com/gitalk/dist/gitalk.min.js"&gt;&lt;/script&gt; &lt;script src="https://cdn.bootcss.com/blueimp-md5/2.10.0/js/md5.min.js"&gt;&lt;/script&gt; &lt;script type="text/javascript"&gt; var gitalk = new Gitalk(&#123; clientID: '&#123;&#123; theme.gitalk.ClientID &#125;&#125;', clientSecret: '&#123;&#123; theme.gitalk.ClientSecret &#125;&#125;', repo: '&#123;&#123; theme.gitalk.repo &#125;&#125;', owner: '&#123;&#123; theme.gitalk.githubID &#125;&#125;', admin: ['&#123;&#123; theme.gitalk.adminUser &#125;&#125;'], id: md5(window.location.pathname), distractionFreeMode: '&#123;&#123; theme.gitalk.distractionFreeMode &#125;&#125;' &#125;) gitalk.render('gitalk-container') &lt;/script&gt;&#123;% endif %&#125; 修改 themes/next/layout/_partials/comments.swig，添加内容如下，与前面的 elseif 同一级别上1234&#123;% elseif theme.gitalk.enable %&#125; &lt;div class="comments" id="comments"&gt; &lt;div id="gitalk-container"&gt;&lt;/div&gt; &lt;/div&gt; 修改 themes/next/layout/_third-party/comments/index.swig，在最后一行添加内容：1&#123;% include 'gitalk.swig' %&#125; 新建 themes/next/source/css/_common/components/third-party/gitalk.styl 文件，添加内容：1234.gt-header a, .gt-comments a, .gt-popup a border-bottom: none;.gt-container .gt-popup .gt-action.is--active:before top: 0.7em; 修改 themes/next/source/css/_common/components/third-party/third-party.styl，在最后一行上添加内容，引入样式：1@import "gitalk"; 在主题配置文件 themes/next/_config.yml 中添加如下内容：12345678gitalk: enable: true # 开启 Gitalk 插件 githubID: xxxx # 存放 Gitalk 评论内容仓库的 Github 账号 repo: xxxx.github.io # 存放 Gitalk 评论内容仓库 ClientID: xxxxx # Github Application Client Id ClientSecret: xxxx # Github Application Client Secret adminUser: xxxx # 管理人员 Github 用户名 distractionFreeMode: true 效果 参考资料 Hexo Next 主题集成本地搜索插件 Hexo NexT主题中集成gitalk评论系统 Hexo搭建博客教程]]></content>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Laravel(Lumen) 5.X 修改时间字段为 INT 类型]]></title>
    <url>%2Fposts%2Flaravel-modify-time-field.html</url>
    <content type="text"><![CDATA[Our predecessors have already encountered problems we may encounter, so it is very important to learn efficient Google Laravel 和 Lumen 框架会自动维护模型 (对应数据库表) 的 created_at、updated_at 和 deleted_at 字段，三个字段非常好用且被框架原生支持，如果要自己实现的话非常的麻烦。但是三个字段在数据库中默认都是 timestamp 或 datetime 类型的，记录一下将三个字段修改为 int 类型的过程。 关于为什么要将 datetime 和 timestamp 两种时间类型替换成 int 类型，可以看一下 CSDN 上的这篇文章: MYSQL数据库时间字段INT, TIMESTAMP, DATETIME性能效率比较。 创建一个测试使用的表1234567CREATE TABLE `test` ( `id` int(10) unsigned NOT NULL AUTO_INCREMENT, `created_at` int(10) unsigned NOT NULL DEFAULT '0', `updated_at` int(10) unsigned NOT NULL DEFAULT '0', `deleted_at` int(10) unsigned NOT NULL DEFAULT '0', PRIMARY KEY (`id`)) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8mb4 COMMENT='测试表'; 修改模型中三个字段默认的时间格式其实如果只是修改三个字段在数据库中保存的数据类型是非常简单的，这个也是被框架原生支持的。 在 Laravel (PS:以下的 Laravel 代表 Laravel 和 Lumen 两个框架，毕竟它们是差不多的) 的模型基类 Illuminate\Database\Eloquent\Model 中引入了很多 Trait, 其中有一个 Trait: Illuminate\Database\Eloquent\Concerns\HasAttributes，该 Trait 中定义了一个 $dateFormat 属性。该属性表示 Laravel 中三个时间字段和 Illuminate\Database\Eloquent\Concerns\HasAttributes 中 $dates 属性所包含字段的时间格式。 默认的 $dateFormat 属性的时间格式为 Y-m-d H:i:s，该字段可被赋值的值的跟 date 函数的第一个值一样。所以如果将该属性的值设置为 &#39;U&#39; (详见 date 函数的 format 准换说明) 当通过模型 create 或 save 的方式创建或修改数据库内容时，created_at、updated_at 和 deleted_at 三个字段的值就会被转换为 UNIX 时间戳。 代码示例 修改默认的时间格式后的模型定义： 1234567891011121314151617181920212223242526&lt;?phpnamespace App;use Illuminate\Database\Eloquent\Model;/** * Class Test * @package App */class Test extends Model&#123; /** * The table associated with the model. * * @var string */ protected $table = 'test'; /** * The storage format of the model's date columns. * * @var string */ protected $dateFormat = 'U';&#125; 使用 tinker 创建一条新的数据并查看数据库内容 将 deleted_at 的默认值修改为非 NULL但是对我个人习惯来说，我喜欢将这三个字段在数据库中的默认值设置为 0 而不是框架原生支持的 NULL，这样做的原因可以看一下在 开源中国 上的一篇文章 mysql 字段设计，尽量设置不允许为null 这样的话框架的原生软删除就会碰到一个问题: 所有数据都是被软删除的! 为了防止这些情况发生，看了一下 Laravel 框架软删除的实现，并做了一些兼容，防止在字段的默认值非 NULL 时获取不到数据。 框架原生的软删除主要是依赖 Illuminate\Database\Eloquent\SoftDeletes 这个 Trait 来实现的。在该 Trait 中引入了一个全局的 Scope：Illuminate\Database\Eloquent\SoftDeletingScope，这个 Scope 中主要定义的就是对软删除数据的过滤、回滚等几个方法的实现。如果我们想要修改对 deleted_at 字段的查询过滤，需要重写该 Scope 的几个方法 重写 Illuminate\Database\Eloquent\SoftDeletingScope 的方法1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465666768697071727374757677787980818283848586878889909192&lt;?phpnamespace App;use Illuminate\Database\Eloquent\Builder;use Illuminate\Database\Eloquent\Model;use Illuminate\Database\Eloquent\SoftDeletingScope as SystemSoftDeletingScope;/** * Class SoftDeletingScope * @package App */class SoftDeletingScope extends SystemSoftDeletingScope&#123; /** * Default value when the resource is not soft deleted * * @var int */ const NOT_DELETE_DEFAULT_VALUE = 0; /** * Apply the scope to a given Eloquent query builder. * Filter soft deleted data by default * * @param \Illuminate\Database\Eloquent\Builder $builder * @param \Illuminate\Database\Eloquent\Model $model * * @return void */ public function apply(Builder $builder, Model $model) &#123; $builder-&gt;where($model-&gt;getQualifiedDeletedAtColumn(), self::NOT_DELETE_DEFAULT_VALUE); &#125; /** * Add the restore extension to the builder. * Add restore extensions to restore soft deleted data back to normal data * * @param \Illuminate\Database\Eloquent\Builder $builder * * @return void */ protected function addRestore(Builder $builder) &#123; $builder-&gt;macro('restore', function (Builder $builder) &#123; $builder-&gt;withTrashed(); return $builder-&gt;update([$builder-&gt;getModel()-&gt;getDeletedAtColumn() =&gt; self::NOT_DELETE_DEFAULT_VALUE]); &#125;); &#125; /** * Add the without-trashed extension to the builder. * Filtering data that has been soft deleted * * @param \Illuminate\Database\Eloquent\Builder $builder * * @return void */ protected function addWithoutTrashed(Builder $builder) &#123; $builder-&gt;macro('withoutTrashed', function (Builder $builder) &#123; $model = $builder-&gt;getModel(); $builder-&gt;withoutGlobalScope($this)-&gt;where($model-&gt;getQualifiedDeletedAtColumn(), self::NOT_DELETE_DEFAULT_VALUE); return $builder; &#125;); &#125; /** * Add the only-trashed extension to the builder. * Add an extension to retrieve only data that has been soft deleted * * @param \Illuminate\Database\Eloquent\Builder $builder * * @return void */ protected function addOnlyTrashed(Builder $builder) &#123; $builder-&gt;macro('onlyTrashed', function (Builder $builder) &#123; $model = $builder-&gt;getModel(); $builder-&gt;withoutGlobalScope($this)-&gt;where($model-&gt;getQualifiedDeletedAtColumn(), '!=', self::NOT_DELETE_DEFAULT_VALUE); return $builder; &#125;); &#125;&#125; 定义模型并验证结果 模型定义: 123456789101112131415161718192021222324252627282930313233343536373839&lt;?phpnamespace App;use Illuminate\Database\Eloquent\SoftDeletes;use Illuminate\Database\Eloquent\Model;/** * Class Test * @package App */class Test extends Model&#123; /** Introduced framework native soft deletes */ use SoftDeletes; /** * Modify the framework native soft delete lock dependent * global scope to be custom scope */ public static function bootSoftDeletes() &#123; static::addGlobalScope(new SoftDeletingScope); &#125; /** * The table associated with the model. * * @var string */ protected $table = 'test'; /** * The storage format of the model's date columns. * * @var string */ protected $dateFormat = 'U';&#125; 使用 tinker 命令软删除一条数据并验证结果 软删除数据后查询 其他的方法和操作就不贴出来了 (PS:怪麻烦的)。总的来说 Laravel 框架及其的好用，但是有些地方为了个人的习惯和逻辑可能需要稍稍的改动。不过在修改的过程中可以学到很多东西，Laravel 作者在设计框架的时候使用了非常多的设计模式。]]></content>
      <tags>
        <tag>Lravel</tag>
        <tag>PHP</tag>
        <tag>Lumen</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Nginx logs]]></title>
    <url>%2Fposts%2Fnginx-logs.html</url>
    <content type="text"><![CDATA[对于 web 服务器来说, 日志是不可缺少的一部分, 对于服务器排错、统计都很有用。nginx 的日志主要分为两种: access_log (访问日志) 和 error_log (错误日志)。通过访问日志我们可以获取到用户的 IP 地址、浏览器信息、请求处理时间等信息，而请求处理时间对于程序员来说是一个及其重要的东西, 毕竟它可以看出来你的代码烂不烂。错误日志记录了访问日志出错的信息, 可以帮我们快速定位错误的位置、原因等重要因素 nginx 错误日志信息介绍配置记录 nginx 的错误信息是调试 nginx 服务的重要手段,属于核心功能模块 ngx_core_module 的参数,该参数名为 error_log ,可以放在 Main 区块中作为全局配置,也可以单独放在不同的虚拟主机中单独记录error_log 的语法格式及参数说明如下: error_log file_path level 关键字 日志文件路径 错误级别 其中,关键字 error_log 不能改变,日志文件路径可以指定任意存放日志的目录,错误级别常见的有 [debug|info|notice|warn|error|crit|alert|emerg] ,级别越高纪录的信息越少,生产环境一般是 [warn|error|crit] 这三个级别之一 注意:尽量不要配置 info 等较低级别的错误级别,这样会带来大量的 I/O 消耗 error_log 的默认值为:1default: error_log logs/error.log error; 可以放置的标签段为1context: main; http; server; location nginx 访问日志介绍nginx 会把每个用户访问网站的日志信息记录到自定的日志文件中,供网站提供者分析用户的浏览行为等。客户端向 nginx 服务器发起的每一次请求都会记录到访问日志中,客户端IP，浏览器信息, referer, 请求处理时间, 请求URL等都可以在访问日志中得到。当然具体要记录哪些信息，你可以通过 log_format 指令定义。此功能由 ngx_http_log_module 模块负责 nginx 访问日志语法:1access_log path [format [buffer=size] [gzip[=level]] [flush=time] [if=condition]]; 参数 说明 path 指定日志的存放位置 format 用来定义记录日志的格式(可以定义多种日志格式,取不同的名字即可) access_log 用来自定日志文件的路径及使用何种日志格式记录日志 buffer 用来指定日志写入时的缓存大小。默认是64k gzip 日志写入前先进行压缩。压缩率可以指定，从1到9数值越大压缩比越高，同时压缩的速度也越慢。默认是1 flush 设置缓存的有效时间。如果超过flush指定的时间，缓存中的内容将被清空 if 条件判断。如果指定的条件计算为0或空字符串，那么该请求不会写入日志 一般场景中,这些参数都不用配置,极端优化时才可能会考虑这些参数 access_log off 中的 off ,表示不记录访问日志 使用 log_format 自定义访问日志格式日志格式定义说明 定义语法1log_format name [escape=default|json] string ...; 如下例:1234access_log /var/logs/nginx-access.log mainlog_format main &apos;$remote_addr - $remote_user [$time_local] &quot;$request&quot; &apos; &apos;$status $body_bytes_sent &quot;$http_referer&quot; &apos; &apos;&quot;$http_user_agent&quot; &quot;$http_x_frowarded_for&quot;&apos; 其中, log_format 为日志格式关键参数,不能随意改变。main 是为日志格式指定的标签,记录日志时会通过这个 main 标签选择指定的格式。标签后面跟着的都是可以记录的日志信息。 具体日志参数信息如下: nginx 日志变量 说明 $remote_addr 记录访问网站的客户端IP地址 $http_x_forwarded_for 当前端游代理服务器时,是指 Web 节点记录客户端地址的配置,此参数生效的前提是代理服务器上也进行了相关的 x_forwarded_for 设置 $remote_user 远程客户端名称 $time_local 记录访问时间与时区 $request 用户的 http 请求起始行信息 $status http 状态码,记录请求返回的状态,例如:200、404、301 等 $body_bytes_sents 服务器发送给客户端的响应的 body 字节数 $http_referer 记录此次请求是从哪个链接访问过来的,可以根据 referer 进行防盗链设置 $http_user_agent 记录客户端访问信息,例如:浏览器、手机客户端等 所有的日志段以空格分割,一行可以记录多个 在没有特殊要求的情况下,采用默认配置即可,更多可以设置的目录日志信息的变量见 nginx日志变量 访问日志可以放置的标签段为1context: http; server; location; if in location; limit_except 更多内容参考 nginx 官方网站 nginx 日志切割默认情况下 nginx 会把所有的访问日志生成到一个指定的访问日志文件 access.log 中,但这样一来,时间长了就导致日志文件内容过大,不利于日志的分析或处理,因此,一般做法会将 nginx 的访问日志按天或按小时进行分割,分成不同的文件保存。 新建一个 shell 脚本 cut_nginx_log.sh,内容如下 12345678#!/bin/shDateFormat=`date +%Y%m%d`LogDir="/application/nginx/logs"LogName="access_default";[ -d $LogDir ] &amp;&amp; cd $LogDir || exit 1[ -f $&#123;LogName&#125;.log ] || exit 1/bin/mv $&#123;LogName&#125;.log $&#123;DateFormat&#125;_$&#123;LogName&#125;.log/bin/touch $&#123;LogName&#125;.log 脚本实现切割 nginx 日志的思想,是将正在写入的 nginx 日志 access_defalut 修改为带日期的的格式文件 xxx_xx_xx_access_default, 然后新建一个 access_default 文件 通过定时任务的方式实现每日 00 点整定时执行 cut_nginx_log.sh 切割日志,将以下内容写入到 /var/spool/cron/root 中12#每日0点切割nginx日志 \00 00 * * * /bin/sh script_path/cut_nginx_log.sh &gt; /dev/null 2&gt;&amp;1 查看 crontab 是否添加成功1234$ crontab -l#每日0点切割nginx日志 \00 00 * * * /bin/sh script_path/cut_nginx_log.sh &gt; /dev/null 2&gt;&amp;1]]></content>
      <tags>
        <tag>Nginx</tag>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello World]]></title>
    <url>%2Fposts%2Fhello-world.html</url>
    <content type="text"><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub. Quick StartCreate a new post1$ hexo new "My New Post" More info: Writing Run server1$ hexo server More info: Server Generate static files1$ hexo generate More info: Generating Deploy to remote sites1$ hexo deploy More info: Deployment]]></content>
  </entry>
</search>
